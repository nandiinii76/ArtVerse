package com.artverse.user;

/**
 * Platform roles. A user may hold more than one (e.g. USER + ARTIST).
 */
public enum Role {
    USER,
    ARTIST,
    GALLERY,
    CURATOR,
    ADMIN,
    MODERATOR
}
